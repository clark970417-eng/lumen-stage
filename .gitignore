node_modules/
dist/
tmp/
.claude/

*.tsbuildinfo
vite.config.js
vite.config.d.ts

.DS_Store
.env
.env.*
!.env.example
.vercel
playwright-report/
test-results/
blob-report/

# Sources the offline converters read. They do not live under public/ any more:
# a .gitignore keeps files out of the repository, and nothing was keeping them
# out of the build — Vite copies all of public/ into dist/, and 306 MB of them
# went up to the deployed site. Staged outside the project while converting.
rocketbox-src/
anim-src/
